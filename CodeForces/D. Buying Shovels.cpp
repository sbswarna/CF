#include<bits/stdc++.h>
using namespace std;
long long tst,n,k;
int main()
{
    cin>>tst;
    while(tst--)
    {
        cin>>n>>k;
        if(k>=n)
            cout<<1<<endl;
        else
        {
            long long ans=999999999999999;
            for(long long i=1;i<=sqrt(k);i++)
            {
                if(n%i==0)
                {

                }
            }
        }
    }
    return 0;
}
